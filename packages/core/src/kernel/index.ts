// Re-export kernel types and utilities, excluding `runDecision` (and its opts type)
// which are already exported by core's own engine.ts to avoid conflicting star exports.
export {
  BudgetManager,
  BudgetReachedError,
  DETERMINISM_SPEC_VERSION,
  DeterminismValidationError,
  HygieneChecker,
  ScenarioLibrary,
  activateDeterministicMode,
  assertValid,
  cacheKey,
  computeDeterministicSeed,
  computeRunSeed,
  computeTranscriptHash,
  createKernelRng,
  deactivateDeterministicMode,
  defaultPruningConfig,
  deterministicNow,
  deterministicTimestamp,
  encodeCanonicalJson,
  generateBranchGraph,
  generateFlipConditions,
  generateId,
  getContractVersionHash,
  getDeterministicContext,
  getDeterministicIdCounter,
  hashAssumptionSet,
  hashDecisionSpec,
  hashInputPayload,
  hygiene,
  interval,
  isDeterministic,
  mergeValidations,
  normalizeFloat,
  pruneGraph,
  requestCacheKey,
  requireProvenanceForFacts,
  scenarios,
  setDeterministicIdCounter,
  sha256,
  stableSort,
  validateFloatBounds,
  validateIROrdering,
  validateNormalizedInput,
  validateOutputHash,
  validateStableSerialization,
  withDeterministicMode,
} from "@zeo/kernel";
