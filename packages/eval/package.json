{
  "name": "@zeo/eval",
  "version": "0.5.1",
  "description": "Evaluation harness for epistemic regressions and determinism checks",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@zeo/contracts": "workspace:*",
    "@zeo/core": "workspace:*",
    "@zeo/replay": "workspace:*",
    "@zeo/tournaments": "workspace:*",
    "@zeo/calibration": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.1"
  }
}
