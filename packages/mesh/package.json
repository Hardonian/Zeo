{
  "name": "@zeo/mesh",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @zeo/kernel build && vitest run"
  },
  "dependencies": {
    "@zeo/kernel": "workspace:*",
    "@zeo/contracts": "workspace:*",
    "@zeo/tenant": "workspace:*",
    "nanoid": "^5.1.16"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "vitest": "^3.2.6"
  }
}
