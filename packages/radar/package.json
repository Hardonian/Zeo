{
    "name": "@zeo/radar",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run",
        "lint": "eslint ."
    },
    "dependencies": {
        "@zeo/contracts": "workspace:*",
        "@zeo/core": "workspace:*",
        "@zeo/signal-discovery": "workspace:*",
        "@zeo/kpi": "workspace:*",
        "@zeo/regimes": "workspace:*",
        "@zeo/lenses": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "^22.10.2",
        "typescript": "^5.7.2",
        "vitest": "^4.1.0"
    }
}