{
  "name": "{{name}}-webhook",
  "version": "{{version}}",
  "description": "ControlPlane webhook runner with security hardening",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node --watch-path dist/index.js",
    "test": "node --experimental-vm-modules node:test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@controlplane/contracts": "latest",
    "@controlplane/optimization-utils": "latest"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
