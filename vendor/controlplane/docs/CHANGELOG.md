# Changelog

## Unreleased
- Hardened runner templates with request IDs, rate limiting, and structured error handling.
- Removed eslint `any` warnings across SDK generator, observability middleware, and benchmark CLI.
- Added contract-test-kit CLI e2e verification and CI dependency audit.
