# Milestones

- v0.1.0 — Scaffold (repo, contracts, core skeleton, CLI demo, docs, CI)
- v0.2.0 — MVP engine loop (composer + branch explorer + explanations)
- v0.3.0 — Evidence ingestion (upload + provenance + constraint extraction)
- v0.4.0 — Negotiation module (game theory lens + robustness UI)
- v0.5.0 — Audio memo pipeline (STT adapter + commitments)
- v0.6.0 — Reality Signal Layer (variables + bias counterweights)

- v0.3.1 — Interface extraction to decouple `@zeo/core` and `@zeo/policy` (shared policy-types package + dependency cleanup)
